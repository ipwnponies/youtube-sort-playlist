# youtube-sort-playlist
Sorts and groups youtube playlist by subscription

## Development

Install `uv`, then run `make venv` to create or refresh
the uv-managed environment from `pyproject.toml` and `uv.lock`. Repo commands run through `uv run`
so they use the synced project environment.
